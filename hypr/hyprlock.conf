source = $HOME/.config/hypr/mocha.conf

$accent = $mauve
$accentAlpha = $mauveAlpha
$font = JetBrainsMono Nerd Font

# GENERAL
general {
    disable_loading_bar = true
    hide_cursor = true
    grace = 3                      # grace period before requiring password
    no_fade_in = false            # enable fade in animation
    no_fade_out = false           # enable fade out animation
    ignore_empty_input = true    # ignore empty password attempts
    immediate_render = false      # render immediately
    pam_module = hyprlock         # PAM module to use
}

# BACKGROUND
background {
  monitor =
  path = $HOME/.config/background
  blur_passes = 0
  color = $base
}

# LAYOUT
label {
  monitor =
  text = Layout: $LAYOUT
  color = $text
  font_size = 25
  font_family = $font
  position = 30, -30
  halign = left
  valign = top
}

# TIME
label {
  monitor =
  text = $TIME
  color = $text
  font_size = 90
  font_family = $font
  position = -30, 0
  halign = right
  valign = top
}

# DATE
label {
  monitor =
  text = cmd[update:43200000] date +"%A, %d %B %Y"
  color = $text
  font_size = 25
  font_family = $font
  position = -30, -150
  halign = right
  valign = top
}

# USER AVATAR
# Commented out - create ~/.face file or replace with your avatar path
# image {
#     monitor =
#     path = $HOME/.face
#     size = 100
#     border_color = $accent
#     border_size = 3
#     rounding = -1               # -1 means completely round
#     rotate = 0
#     reload_time = -1           # reload image every X seconds (-1 never)
#     reload_cmd =               # command to get new path
#     position = 0, 75
#     halign = center
#     valign = center
#     shadow_passes = 1
#     shadow_size = 3
#     shadow_color = rgba(0, 0, 0, 1.0)
# }

# INPUT FIELD
input-field {
    monitor =
    size = 350, 65
    outline_thickness = 4
    dots_size = 0.25
    dots_spacing = 0.25
    dots_center = true
    dots_rounding = -1             # -1 default circle, -2 follow input-field rounding
    outer_color = $accent
    inner_color = $surface0
    font_color = $text
    font_family = $font
    fade_on_empty = false
    fade_timeout = 2000
    placeholder_text = <span foreground="##$textAlpha"><i>󰌾 Logged in as </i><span foreground="##$accentAlpha">$USER</span></span>
    hide_input = false
    rounding = 15
    check_color = $accent
    fail_color = $red
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
    fail_timeout = 2000
    fail_transition = 300
    capslock_color = $yellow
    numlock_color = $yellow
    bothlock_color = $yellow
    invert_numlock = false
    swap_font_color = false
    position = 0, -47
    halign = center
    valign = center
    shadow_passes = 1
    shadow_size = 3
    shadow_color = rgba(0, 0, 0, 0.5)
}

# BATTERY STATUS (if laptop)
label {
    monitor =
    text = cmd[update:5000] echo "🔋 $(cat /sys/class/power_supply/BAT*/capacity 2>/dev/null || echo 'N/A')%"
    color = $text
    font_size = 20
    font_family = $font
    position = 30, 30
    halign = left
    valign = bottom
}

# UPTIME
label {
    monitor =
    text = cmd[update:60000] uptime -p | sed 's/up //' 
    color = $subtext1
    font_size = 16
    font_family = $font
    position = -30, 30
    halign = right
    valign = bottom
}

# WEATHER (optional - requires curl)
label {
    monitor =
    text = cmd[update:1800000] curl -s "wttr.in/?format=1" 2>/dev/null || echo "Weather: N/A"
    color = $subtext1
    font_size = 18
    font_family = $font
    position = 0, 30
    halign = center
    valign = bottom
}

# CAPS LOCK INDICATOR
label {
    monitor =
    text = CAPS
    color = $yellow
    font_size = 14
    font_family = $font
    position = 0, -150
    halign = center
    valign = center
    shadow_passes = 1
    shadow_size = 2
    shadow_color = rgba(0, 0, 0, 0.8)
}